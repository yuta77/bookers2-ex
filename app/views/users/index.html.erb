<div class="row">
  <div class="col-xs-3">
    <h2>User info</h2>
    <%= render 'users/profile' %>

    <h2>New book</h2>
    <%= render 'books/form' %>
  </div>

  <div class="col-xs-9">
    <%# アクションが #index なら "Users" それ以外は "アクション名" %>
    <h2><%= params[:action] == "index" ? "Users" : params[:action] %></h2>

    <%= render 'users/list', users: @users %>
  </div>
</div>
